#defmodule GrpowWeb.PowEmail do
  #import Bamboo.Email

  #def reset_password_email do
    #new_email()
      #|> to(user.email)
      #|> from("grpow@example.com")
      #|> subject(subject)
      #|> html_body(html)
      #|> text_body(text)
  #end
#end
